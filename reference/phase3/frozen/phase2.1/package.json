{
  "name": "api-mcp-negative-result-warrant-phase-2-1",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "description": "A receiver-validated, proposition-bound negative evidence experiment.",
  "scripts": {
    "test": "node --experimental-strip-types --test --test-reporter=spec tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit && node --experimental-strip-types --test --test-reporter=spec tests/*.test.ts"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@modelcontextprotocol/server": "2.0.0",
    "@types/node": "24.3.0",
    "typescript": "5.9.2"
  }
}
